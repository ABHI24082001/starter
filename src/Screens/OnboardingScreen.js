import React from 'react';
import OnboardingCarousel from '../components/OnboardingCarousel';

const OnboardingScreen = ({ navigation }) => {
  return <OnboardingCarousel navigation={navigation} />;
};

export default OnboardingScreen;
